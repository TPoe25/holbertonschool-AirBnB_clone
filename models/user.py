#!/bin/usr/python3
""" defines the user class"""

import BaseModel


class User(BaseModel):
    """
    class for the users email, password, first_name, last_name
    
    """
    
    
    def __init__(self):
        super().__init__(self)
        
        User = BaseModel()
        User.BaseModel()
